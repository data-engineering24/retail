{
	"name": "DF_let_SalesDim",
	"properties": {
		"type": "Flowlet",
		"typeProperties": {
			"sources": [
				{
					"name": "source1"
				}
			],
			"sinks": [],
			"transformations": [
				{
					"name": "join1"
				},
				{
					"name": "select1"
				},
				{
					"name": "assert1"
				},
				{
					"name": "input1"
				}
			],
			"scriptLines": [
				"input(output(",
				"          executed_time as timestamp",
				"     ),",
				"     order: 0,",
				"     allowSchemaDrift: true) ~> input1",
				"source(allowSchemaDrift: true,",
				"     validateSchema: false) ~> source1",
				"select1, input1 join(ERROR_FUNCTION('') == ERROR_FUNCTION(''),",
				"     joinType:'outer',",
				"     matchType:'exact',",
				"     ignoreSpaces: false,",
				"     broadcast: 'auto')~> join1",
				"source1 select(skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> select1",
				"input1 assert(expectTrue(ERROR_FUNCTION(''), false, 'assert1')) ~> assert1"
			]
		}
	}
}